<!DOCTYPE html>
<html>

    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="/static/css/base.css" rel="stylesheet">
        <style>
         body {
             font-family: Arial, Helvetica, sans-serif;
             background-color: #f9f9f9;
         }

         form {
             border: 3px solid #f1f1f1;
             padding: 16px;
         }

         h1, h2, p {
             text-align: center;
         }

         img.logo {
             display: block;
             margin-left: auto;
             margin-right: auto;
             width: 80px;
             border: 2px solid transparent;
             border-radius: 50%;
             overflow: hidden;
         }

         .container {
             margin: auto;
             padding-top: 100px;
             width: 400px;
             height: 400px;
         }

        </style>
    </head>

    <body>

        <div class="container">
            <img class="logo" src="${request.static_url('nti.app.environments:static/logo.png')}"/>
            <h1>${message}</h1>
	    <h2>Oops...</h2>
	    <metal:block tal:omit-tag="" tal:condition="subtext">
                <p>
                    ${subtext}
                </p>
            </metal:block>
	    <p>If you feel this is in error contact <a href="mailto:support@nextthought.com">support@nextthought.com</a></p>
        </div>

    </body>
</html>
