<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
        }

        form {
            border: 3px solid #f1f1f1;
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            cursor: pointer;
            width: 100%;
            font-size: 14px;
            font-weight: 800;
        }

        button:hover {
            opacity: 0.8;
        }

        .container {
            margin:auto;
            width: 400px;
            height: 400px;
        }
        .form-container {
            padding: 16px;
        }

    </style>
</head>

<body>

    <div class="container">
        <h2>Welcome to NextThought</h2>
        <form action="${rel_logon}" method="get">
            <div class="form-container">
                <button type="submit">Sign in with Google</button>
            </div>
        </form>
    </div>

</body>

</html>